# Import des flows

1. Ouvrir Node-RED sur http://localhost:1880
2. Menu > Import > sélectionner `jarvis-v1.flows.json`
3. Deploy

Alternative API:
```bash
jarvis/scripts/nodered/deploy_flows_via_api.sh
```
